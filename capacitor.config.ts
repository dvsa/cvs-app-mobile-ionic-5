import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'uk.gov.dvsa.cvsmobileapp',
  appName: 'Vehicle Testing',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
